export const fonts = {
    neutralFace: (color, size) => ({
        fontSize: size,
        color: color,
        fontFamily: 'Neutral',
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 'normal'
    }),

    textAlign: (position) => ({
        textAlign: position
    }),

    Mazius: (size) => ({
        color: '#FFF',
        fontFamily: 'Mazius',
        fontSize: size,
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 'normal',
    }),

    Avenir: (size) => ({
        color: '#FFF',
        fontFamily: 'Avenir',
        fontSize: size,
        fontStyle: 'normal',
        fontWeight: 400,
        lineHeight: 'normal',
    })
}

export const styles = {
    
}